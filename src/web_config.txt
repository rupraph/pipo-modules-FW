Sensor mode: 
    - independant voices: choose sensor functions to enable/disable
        - roll
        - pitch
        - yaw

        - acc X
        - acc Y
        - acc Z
        Advanced:   - sensor settings
                    - sensitivity / scale
    - auto process: detect tap. movement....

=> sensor class provides bool or double data streams. 

HW settings:
- Select MODE: midi or controller
- Enable/disable OSC
- OSC settings (enabled IP, port, ...)
(- rtp ?)

-> transports
    - angles to basic midi (usb/ble/rtp)
        if CC: basic or hiRes
            advanced ?
        if note: select scale.
            advanced: custom scale

    - angles to OSC        (osc only)
        Advanced: custom ranges

    - angles to Joystick
        Advanced
     

