{
  "name": "pipo-single",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "build:motion": "./scripts/pio.sh run -e motion",
    "build:analog": "./scripts/pio.sh run -e analog",
    "build:range": "./scripts/pio.sh run -e range",
    "build:web": "./scripts/build-web",
    "upload:range": "./scripts/pio.sh run -t upload -e range",
    "upload:motion": "./scripts/pio.sh run -t upload -e motion",
    "upload:analog": "./scripts/pio.sh run -t upload -e analog",
    "upload": "npm run upload:range",
    "fs:motion": "./scripts/pio.sh run -t buildfs -e motion",
    "fs:analog": "./scripts/pio.sh run -t buildfs -e analog",
    "fs:range": "./scripts/pio.sh run -t buildfs -e range",
    "fs": "./scripts/pio.sh run -t buildfs",
    "upload:fs": "./scripts/pio.sh run -t uploadfs",
    "upload:all": "npm run upload &&npm run fs &&npm run upload:fs",
    "updload-web": "npm run build:web && npm run fs && npm run upload:fs",
    "all": "npm run fs && npm run upload:all"
  },
  "author": "",
  "license": "ISC"
}
